# API requests are proxied via functions/api/[[path]].js (Cloudflare Pages Function)
#
# Missing /assets/* files are turned into real 404s by functions/assets/[[path]].js,
# NOT here: Cloudflare Pages only honours 301/302/303/307/308 and 200 rewrites in
# this file, so a `404` rule is silently ignored and the catch-all below wins.
# Without that guard a stale bundle hash comes back as index.html with a 200,
# which the browser rejects as CSS/JS and the service worker caches as the asset.

# SPA fallback - all non-file routes serve index.html
/* /index.html 200
